# Private data — never commit raw audio or drafts (see CLAUDE.md guardrails)
data/
clips/
*.wav
*.mp3
*.m4a
manifest.jsonl
*_verified.jsonl
dataset/
text/

# Python
.venv/
venv/
__pycache__/
*.pyc

# OS / editor
.DS_Store

# Kutchi-GPT corpora, tokenizer, shards, checkpoints (large, local-only)
data_lm/
lm/**/checkpoints/
